.next
node_modules
storage
.env
.env.local
coverage
dist
*.log
*.tsbuildinfo

# Runbook trien khai chua chi tiet truy cap root cua server production.
# Repo nay public nen giu file o local.
docs/deploy-unique-visitor-worker.md

# MediaMTX dev harness: config that RTSP credentials + video ghi hinh local.
mediamtx.dev.yml
recordings/
mediamtx

# Python face-matching worker (venv + caches + runtime secret)
.venv/
__pycache__/
*.pyc
.pytest_cache/
face-matching/worker.env
